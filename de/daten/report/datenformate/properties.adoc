= properties
:lang: de
include::{includedir}/_header.adoc[]
:keywords: properties, Eigenschaften, Report, Rohdaten
:position: 163
:url: daten/report/datenformate/properties

:datenformat: pass:quotes[*properties*]
:daten: die im System vorhandenen Eigenschaften
:spezifikationen: <<tabelle-spezifikationen-properties>>
:datenspalten: <<tabelle-datenspalten-properties>>

include::./_textblocks/einleitung.adoc[]

== Technische Spezifikationen

include::./_textblocks/spezifikationen.adoc[]

[[tabelle-spezifikationen-properties]]
.Technische Spezifikationen des Formats *properties*
[cols=4]
|====
|Eigenschaft |Bedeutung |Wert |Erläuterung

|dataName
|Name
|properties
|Name des Creator-Prozesses und Präfix der erstellten Datei. +
Dateiname: *properties-xxx.csv.gz*

|fileFormat
|Dateiformat
|CSV, GZIP
|Die Daten werden als per GZIP (RFC 1952) komprimierte CSV-Datei bereitgestellt.

|limitPage
|Zeilen pro Anfrage
|bis zu 1.000
|Die maximale Anzahl der Datenzeilen, die in eine einzelne Datei geschrieben wird.

|limitDay
|Zeilen pro Tag
|bis zu 2.000
|Die maximale Anzahl der Datenzeilen, die insgesamt an einem Tag geschrieben wird.

|earliestPossibleDataDate
|Frühester Zeitpunkt
|2016-01-01
|Zeitpunkt, ab dem die Rohdaten erhoben werden können. Die Daten werden nicht gefiltert.

|deleteRawDataAfterDaysMinimum
|Minimale Anzahl an Tagen bis zur Löschung
|10
|Die minimale Anzahl an Tagen, bis die generierte Rohdaten-Datei wieder vom Cloud-Speicher entfernt wird. Eine Weiterverarbeitung muss somit vorher erfolgen. Die genaue Dauer kann im Menü *Daten » Rohdaten* definiert werden.

|deleteRawDataAfterDaysMaximum
|Maximale Anzahl an Tagen bis zur Löschung
|60
|Die maximal Anzahl an Tagen, bis die generierte Rohdaten-Datei wieder vom Cloud-Speicher entfernt wird. Eine Weiterverarbeitung muss somit vorher erfolgen. Die genaue Dauer kann im Menü *Daten » Rohdaten* definiert werden.

|filterType
|Filtertyp
|lastUpdate
|Daten werden basierend auf der letzten Aktualisierung gefiltert.

|dataUpdatedAtColumnName
|Spaltenbezeichnung
|prop_updated_at
|Name der Spalte, die den Zeitstempel der letzten Aktualisierung der Daten enthält.

|defaultActive
|Standardmäßig aktiv
|nein
|Der Export läuft erst dann automatisch im Hintergrund, wenn der Creator zuvor im Menü *Daten » Rohdaten* aktiviert wurde.

|interval
|Intervall
|täglich
|Mit dieser Einstellung werden 1 x pro Tag, gewöhnlich über Nacht, alle seit dem letzten Durchlauf geänderten Daten exportiert.

|====

== Datenspalten

include::./_textblocks/datenspalten.adoc[]

[[tabelle-datenspalten-properties]]
.Datenspalten des Formats *properties*
[cols="1,3,3,3"]
|====
|Eigenschaft |Datentyp |Vollständige Bezeichnung |Erläuterung

|plenty_id
|INTEGER NOT NULL
|plenty id
|ID des plentymarkets Systems

|*prop_id (Primary key)*
|VARCHAR NOT NULL
|property ID
|ID der Eigenschaft, wie in der <<artikel/einstellungen/eigenschaften#600, Eigenschaftskonfiguration>> angegeben.

|prop_cast
|VARCHAR
|property cast
|Bereich in dem die Eigenschaft genutzt wird, wie in der <<artikel/einstellungen/eigenschaften#600, Eigenschaftskonfiguration>> angegeben.

|prop_type
|VARCHAR
|property type
|Eigenschaftstyp, wie in der <<artikel/einstellungen/eigenschaften#600, Eigenschaftskonfiguration>> angegeben.

|prop_position
|INTEGER
|property position
|Positionsnummer der Eigenschaft, wie in der <<artikel/einstellungen/eigenschaften#600, Eigenschaftskonfiguration>> angegeben.

|prop_name
|VARCHAR
|property name
|Name der Eigenschaft, wie in der <<artikel/einstellungen/eigenschaften#600, Eigenschaftskonfiguration>> angegeben.

|prop_created_at
|TIMESTAMPTZ
|property created at
|Zeitstempel der Anlage der Eigenschaft +
Datumsformat: MM/DD/YY HH:MI:SS TZ

|*prop_updated_at*
|TIMESTAMPTZ
|property updated at
|Zeitstempel der letzten Aktualisierung der Eigenschaft +
Datumsformat: MM/DD/YY HH:MI:SS TZ

|====
